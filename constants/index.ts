// Application constants
export const API_ROUTES = {
  AUTH: '/api/auth',
  QR_CODES: '/api/qr-codes',
  SCAN: '/api/scan',
} as const;

export const ROUTES = {
  HOME: '/',
  LOGIN: '/login',
  REGISTER: '/register',
  DASHBOARD: '/dashboard',
  SCAN: '/scan',
} as const;

